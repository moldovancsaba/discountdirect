import { errorResponse } from "@/auth/http"; import { HandoffError } from "./service";
export function handoffError(error:unknown){ if(error instanceof HandoffError){const map={NOT_FOUND:["HANDOFF_NOT_FOUND","A kérelmezett átadás nem található.",404],FORBIDDEN:["FORBIDDEN","Ehhez az ajánlathoz nincs hozzáférésed.",403],INVALID:["HANDOFF_INVALID","Az átadási kérés érvénytelen.",400],CONFLICT:["OFFER_NOT_ACCEPTED","Csak elfogadott és érvényes ajánlat adható át.",409],EXPIRED:["HANDOFF_EXPIRED","Az átadási hivatkozás lejárt.",410],USED:["HANDOFF_USED","Az átadási hivatkozást már felhasználták.",409],UNAVAILABLE:["CHECKOUT_UNAVAILABLE","A webáruházi átadás átmenetileg nem érhető el.",503]} as const; const [code,message,status]=map[error.code]; return errorResponse(code,message,status);} return errorResponse("HANDOFF_UNAVAILABLE","A webáruházi átadás átmenetileg nem érhető el.",503); }
