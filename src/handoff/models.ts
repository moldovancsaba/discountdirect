import mongoose from "mongoose"; import { sellerScopedSchema } from "../lib/tenant-core.ts";
const { Schema, model, models } = mongoose;
const schema = new Schema({ sellerId:{type:Schema.Types.ObjectId,required:true,ref:"Seller",index:true}, offerId:{type:Schema.Types.ObjectId,required:true,ref:"Offer",unique:true}, buyerUserId:{type:Schema.Types.ObjectId,required:true,ref:"User",index:true}, priceHuf:{type:Number,required:true,min:0}, nonceHash:{type:String,required:true,unique:true}, status:{type:String,enum:["issued","processing","consumed","expired","revoked","failed"],default:"issued",index:true}, expiresAt:{type:Date,required:true,index:true}, processingStartedAt:{type:Date,default:null}, consumedAt:{type:Date,default:null}, provider:{type:String,default:null}, providerReference:{type:String,default:null,maxlength:180}, checkoutUrlHash:{type:String,default:null}, lastErrorCode:{type:String,default:null,maxlength:80}, version:{type:Number,required:true,default:1,min:1} },{timestamps:true,versionKey:false,collection:"offer_handoffs"});
schema.plugin(sellerScopedSchema); export const OfferHandoff=models.OfferHandoff||model("OfferHandoff",schema); export const handoffModels=[OfferHandoff];
