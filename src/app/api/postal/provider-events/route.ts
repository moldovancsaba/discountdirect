import { errorResponse } from "@/auth/http";import { handlePostalProviderEvent,PostalProviderError } from "@/postal/provider";
export const runtime="nodejs";export const dynamic="force-dynamic";
export async function POST(request:Request){try{const payload=await request.text();if(!payload||payload.length>64_000)return errorResponse("INVALID_POSTAL_EVENT","Érvénytelen postai esemény.",400);return Response.json(await handlePostalProviderEvent(payload,request.headers),{headers:{"Cache-Control":"no-store"}});}catch(error){const status=error instanceof PostalProviderError&&error.code==="UNAVAILABLE"?503:400;return errorResponse("POSTAL_EVENT_REJECTED","A postai esemény nem dolgozható fel.",status);}}
