"use server";
import { redirect } from "next/navigation";
import { currentUser } from "@/auth/service";
import { cancelCampaign, createFlashCampaignPreview, launchFlashCampaignPreview } from "@/campaigns/service";
async function identity() { const user = await currentUser(); if (!user) redirect("/sign-in"); return user; }
export async function previewFlashCampaignAction(sellerSlug: string, form: FormData) { const user = await identity(); let target = `/seller/${sellerSlug}/campaigns?error=INVALID`; try { const preview = await createFlashCampaignPreview(user.id, sellerSlug, { productId: form.get("productId"), discountPct: Number(form.get("discountPct")), quantity: Number(form.get("quantity")), expiresAt: form.get("expiresAt"), channel: form.get("channel") }); target = `/seller/${sellerSlug}/campaigns?preview=${preview.id}`; } catch (error) { target = `/seller/${sellerSlug}/campaigns?error=${encodeURIComponent(error instanceof Error ? error.message : "INVALID")}`; } redirect(target); }
export async function launchFlashCampaignAction(sellerSlug: string, previewId: string) { const user = await identity(); let target = `/seller/${sellerSlug}/campaigns?saved=created`; try { await launchFlashCampaignPreview(user.id, sellerSlug, previewId, crypto.randomUUID().replaceAll("-", "")); } catch (error) { target = `/seller/${sellerSlug}/campaigns?preview=${previewId}&error=${encodeURIComponent(error instanceof Error ? error.message : "INVALID")}`; } redirect(target); }
export async function cancelCampaignAction(sellerSlug: string, campaignId: string) { const user = await identity(); let target = `/seller/${sellerSlug}/campaigns?saved=cancelled`; try { await cancelCampaign(user.id, sellerSlug, campaignId); } catch (error) { target = `/seller/${sellerSlug}/campaigns?error=${encodeURIComponent(error instanceof Error ? error.message : "INVALID")}`; } redirect(target); }
