# Never commit real values. GITHUB_TOKEN is also needed during package installation.
MONGODB_URI=
MONGODB_DB=discountdirect
APP_URL=http://localhost:3000
GITHUB_TOKEN=
# Temporary operator access until individual administrator accounts ship; >=32 characters.
OPERATIONS_TOKEN=
SSO_CLIENT_ID=
SSO_CLIENT_SECRET=
SSO_ORIGIN=https://sso.doneisbetter.com
SSO_REDIRECT1_URI=http://localhost:3000/auth/callback
SSO_REDIRECT2_URI=http://localhost:3000/api/oauth/callback
