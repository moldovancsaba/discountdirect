import test from "node:test";import assert from "node:assert/strict";import {ARTIFACT_MAX_BYTES,retentionUntil,validateArtifact} from "../src/artifacts/core.ts";
test("artifact validation hashes bounded content and enforces kind MIME",()=>{const result=validateArtifact("letter-pdf",new TextEncoder().encode("pdf"),"application/pdf");assert.equal(result.size,3);assert.match(result.sha256,/^[a-f0-9]{64}$/);assert.throws(()=>validateArtifact("letter-pdf",new Uint8Array(),"application/pdf"),/ARTIFACT_SIZE_INVALID/);assert.throws(()=>validateArtifact("letter-pdf",new TextEncoder().encode("x"),"application/json"),/ARTIFACT_TYPE_INVALID/);});
test("artifact retention is deterministic by kind",()=>{const now=new Date("2026-09-19T00:00:00Z");assert.equal(retentionUntil("privacy-export",now).toISOString(),"2026-09-26T00:00:00.000Z");assert.equal(retentionUntil("letter-pdf",now).toISOString(),"2027-09-19T00:00:00.000Z");assert.equal(ARTIFACT_MAX_BYTES,10*1024*1024);});
